// lib/appIcon.js — native alternate-icon çağrıları kapatıldı (TestFlight çökmesi).
export function isNightLocal() {
  return false;
}

export async function syncAppIconTheme() {
  return;
}

export function watchAppIconTheme() {
  return () => {};
}
